{
  "_comment": "The single source of truth for what is wrong with Hangyul ganada. Hand-edited; docs/report.md's issue tables are GENERATED from this by scripts/build-issues.mjs, and `npm run issues:check` fails the build if they have drifted or if any prose elsewhere in the report contradicts a status here. Nothing else in the repository may state an issue's status, and no other table may restate the list — the report's what/how/next tables and its counts are all generated from here.",
  "statuses": {
    "OPEN": "Not fixed. The customer can still meet it.",
    "PARTIAL": "The common case is fixed and a real one is not.",
    "RESOLVED": "Fixed, with evidence a reader can re-run.",
    "BLOCKED": "Cannot be fixed from this repository. The blocker is named."
  },
  "severities": [
    "P0",
    "P1",
    "P2",
    "P3"
  ],
  "issues": [
    {
      "id": "I-01",
      "area": "Release",
      "severity": "P0",
      "status": "OPEN",
      "summary": "The shipped APK/AAB predate the current product code by one commit",
      "customerImpact": "Anyone installing the delivered binary today gets the previous stroke geometry and the retired video splash. The eight syllables re-measured in `e026697` — 구 오 밤 밥 옷 국 꽃 글 — render from the older table, and the launch screen is the MP4 clip the product has stopped shipping.",
      "evidence": "Fourth occurrence. `app_result/build-info.json` names commit `557edfb`; HEAD is `e026697`, and the one commit between them changed product code, not documents: `compose.ts`, `strokes.ts`, `strokeVectors.ts`, `generated/composition.json`, `LaunchSplash.tsx`, `AppShell.module.css`, `AppHeader.tsx`, the design tokens, the splash artwork and the Android/iOS launch bitmaps. Unpacked from the delivered APK this cycle: `assets/public/brand/splash/splash-ko.mp4`, `splash-en.mp4` and both WebP posters are present — files deleted in `e026697` — and the PNG splash that replaced them is absent. `curriculum-data-*.js` inside the package carries `국:{aspect:.9669,parts:[[.1257,0,.8686,.2597],…]}`, the pre-`e026697` measurement; the current table has `국` at `[.1257,0,.8686,.3646]` with a `cut` field the package never mentions. The same is true of the project snapshots delivered beside the binaries: `result/android-project/.../assets/public/brand/splash/` holds the two MP4s, and the iOS and Android launch bitmaps in `result/` differ by SHA-256 from the ones in the working tree.",
      "recommendedFix": "Rebuild the web bundle, `cap sync`, and re-sign from HEAD with the working tree clean, then re-grep the delivered package for the two markers that distinguish the cycles: no `splash-*.mp4` under `assets/public/brand/splash/`, and `국` measuring `.3646` rather than `.2597`. Outside this audit — no artefact was rebuilt for it.",
      "effort": "LOW — one build and one signing run, once the build is allowed",
      "note": "Written for the fourth time. The three previous cycles each closed this row and each shipped a binary that predated the report describing it. The pattern is not the build failing; it is product work landing after the build and the report not being re-read against the delivered package."
    },
    {
      "id": "I-02",
      "area": "Repo",
      "severity": "P0",
      "status": "RESOLVED",
      "summary": "A whole cycle's work was uncommitted when the artefacts were built",
      "customerImpact": "A fresh checkout does not contain what was shipped",
      "evidence": "Committed before the build, in that order, this cycle and the two before it.",
      "recommendedFix": "done",
      "keepResolved": "The other most-repeated finding. Same reason."
    },
    {
      "id": "I-23",
      "area": "Strokes",
      "severity": "P0",
      "status": "RESOLVED",
      "summary": "The stroke demonstration showed ownership wedges at junctions and a polygonal ㅇ",
      "customerImpact": "ㅂ's uprights grew triangular spurs into crossbars that had not been written yet; ㅅ's first stroke grew a chunk of the second one's shoulder; ㅈ chipped into its own fork; ㅇ read as a lumpy ring rather than a circle. A learner watching stroke one of ㅂ could see a piece of stroke three already on the paper.",
      "evidence": "Reproduced by rendering the shipped assets before any change was made. Fixed by replacing the architecture — see the entry for it in §11. Now: `strokes:qa` clean on 73 items / 269 strokes; `strokes:visual` clean on 1,345 frames; the gallery read by eye at 160 px and at 96 px, which is the size the defect was reported at.",
      "recommendedFix": "done",
      "supersedes": "I-14",
      "note": "The previous report called the stroke renderer RELEASE QUALITY and said all 73 items had been visually inspected. Both statements were false, and the customer's screenshots showed it. See §11.2."
    },
    {
      "id": "I-03",
      "area": "Product",
      "severity": "P1",
      "status": "BLOCKED",
      "summary": "The Hangyul hand-off is built but has no destination",
      "customerImpact": "A learner who finishes the alphabet finishes the product and stops. The card and the My Learning row render nothing rather than leading nowhere.",
      "evidence": "`HANGYUL_URL` is null in a plain checkout; `NextStepCard` returns null; `routing:check` reports which way a build went. Searching both repositories on this machine finds the main product — the Expo app `Hangyul`, bundle `com.hangyul.app`, scheme `hangyul` — and its backend `api.talkhangyul.com`, and this app's own host `ganada.talkhangyul.com`. Neither repository declares a learner-facing web address for the main app. The one occurrence of `https://hangyul.app` is a fallback inside a `catch` in a billing modal, not a declared destination.",
      "recommendedFix": "Whoever owns the product supplies the destination — a landing page, a store listing or a universal link — and it is set as `VITE_HANGYUL_URL` at build time. Documented in `.env.example`.",
      "blocker": "The value is not in this repository and must not be guessed.",
      "effort": "LOW — one environment variable, once the value exists"
    },
    {
      "id": "I-04",
      "area": "Vocabulary",
      "severity": "P1",
      "status": "OPEN",
      "summary": "2,581 of a stated 10,000 words",
      "customerImpact": "Buyers compare corpus size",
      "evidence": "`vocabulary:qa:check` reports the shortfall against the target.",
      "recommendedFix": "Decide I-05 first, then author. Authoring into the current delivery model makes I-05 worse.",
      "effort": "HIGH (content)"
    },
    {
      "id": "I-05",
      "area": "Performance",
      "severity": "P1",
      "status": "OPEN",
      "summary": "The corpus at 10,000 words is three times the bundle budget",
      "customerImpact": "The delivery architecture cannot carry the stated plan",
      "evidence": "`bundle:budget` forecasts 663.7 kB gzipped against a 220 kB budget from the measured 68 B/word. Not enforced; gated at 4,000 headwords.",
      "recommendedFix": "Chunk the corpus by frequency band, split per locale, precache the shell and one locale and cache the rest on use.",
      "effort": "MEDIUM — chunking and a cache policy"
    },
    {
      "id": "I-24",
      "area": "Handwriting",
      "severity": "P2",
      "status": "OPEN",
      "summary": "The traced guide is smaller than the demonstration for a single letter",
      "customerImpact": "On a letter lesson the grey glyph a learner traces fills about two-thirds of the writing square while the demonstration below it fills 0.84 of its own, and it does not sit on the crosshair drawn under it. Same letter, two sizes, one screen. It also costs accuracy: on Pretendard, the default face, 1.04% of correct attempts are rejected — five times the overall average — and every one of those rejections is a letter written *small and drifted*, which is what tracing a small off-centre guide produces.",
      "evidence": "Measured again this cycle, on ㅏ, off the running app: the grey guide the learner traces occupies 0.228 × 0.672 of the writing square and is centred at (0.556, 0.460) rather than (0.5, 0.5), while the taught glyph for the same letter is 0.251 × 0.840 and dead-centre. For ㄱ the gap is wider — Pretendard sets an isolated ㄱ at 0.53 × 0.42 of the box, eight per cent above its centre, against a taught 0.84 × 0.664. So the guide is both smaller and off-centre from the crosshair drawn under it. Scaling the glyph to match was implemented and reverted: it scales the glyph's stroke too, from 0.057 of the box to 0.090 against a fixed 0.062 pen, and the robustness corpus went from 0.21% false rejections to 21% — one correct letter in five told it was wrong. See `features/writing/glyphSpec.ts`. Re-run of `handwriting:robustness` this cycle isolates the cost: Pretendard frr 0.01042 against an overall 0.00208, with the five rejected cases being ㄴ, ㄷ, ㅁ, ㅂ 'small and drifted' and ㄹ 'stopped short'. The same run corrected a labelling error carried by several reports — 0.21% is the false-*reject* rate and 0.78% the false-*accept* rate, not the other way round (§12.2).",
      "recommendedFix": "A grading model that does not measure coverage against a stroke the pen cannot fill, then the glyph can be sized for the eye. Its own calibration, not a side effect of a layout change.",
      "effort": "MEDIUM–HIGH — a grading recalibration with its own corpus run"
    },
    {
      "id": "I-25",
      "area": "Build",
      "severity": "P2",
      "status": "OPEN",
      "summary": "`strokes:measure:check` is not on the release gate",
      "customerImpact": "None directly. The table is now reproducible and the check exists, but nothing runs it automatically, so a face upgrade could move the measurements without anyone being told.",
      "evidence": "The original defect — `npm run strokes:measure` rewriting 23 of 33 syllables — was the script measuring a *fallback* face: the app ships Pretendard as a dynamic subset, and `document.fonts.load(font)` with no text loads only the Latin ranges, so `fillText` drew whatever the system had and the result depended on which subsets the app's own first screen had warmed. The script now loads the face for the text it measures, the table has been re-derived, and all 33 syllables were read by eye against the reference glyph. `npm run strokes:measure -- --check` passes repeatedly. What is left is that it is not in `verify:release`, because unlike every other gate it needs the app served on :4477.",
      "recommendedFix": "Either have `measure-composition.mjs` start its own preview server when nothing is listening, or add the check to a release step that already has one, and then put it in `verify:release` beside `jamo:measure:check`.",
      "effort": "LOW — one script change, or one gate entry"
    },
    {
      "id": "I-06",
      "area": "Word Detail",
      "severity": "P1",
      "status": "RESOLVED",
      "summary": "Longer explanations were English-only dictionary scrapings",
      "customerImpact": "Non-English learners never saw the block; English learners read \"phylum\" under 문",
      "evidence": "25 written words in ten languages; §15.2.",
      "recommendedFix": "done"
    },
    {
      "id": "I-07",
      "area": "Vocabulary",
      "severity": "P1",
      "status": "RESOLVED",
      "summary": "Vietnamese and Thai vocabulary covered 500 of 2,581 words",
      "customerImpact": "Past word 500 a vi/th learner read marked English",
      "evidence": "2,581 non-null rows in both.",
      "recommendedFix": "done"
    },
    {
      "id": "I-08",
      "area": "Content",
      "severity": "P1",
      "status": "RESOLVED",
      "summary": "Entries whose gloss contradicted their own example",
      "customerImpact": "열 read \"fever\" above a sentence about counting to ten",
      "evidence": "Eleven found, all authored and pinned; `vocabulary:sense:qa:check` passes.",
      "recommendedFix": "done"
    },
    {
      "id": "I-09",
      "area": "Vocabulary UX",
      "severity": "P2",
      "status": "PARTIAL",
      "summary": "No matching exercise; production is tiles, not a keyboard",
      "customerImpact": "Vocabulary still feels mostly like recognition on cards",
      "evidence": "`build` was added; matching is still absent. Matching spans four words and the learning plan is per-word.",
      "recommendedFix": "A session-level group exercise that can cover several word ids while crediting each word's evidence once. A scheduling change, not a screen.",
      "effort": "MEDIUM — a session-level exercise concept, not a screen"
    },
    {
      "id": "I-10",
      "area": "Content",
      "severity": "P2",
      "status": "PARTIAL",
      "summary": "Korean and English glosses describe different senses for some polysemous words",
      "customerImpact": "The meaning changes when the interface language changes",
      "evidence": "The eleven known cases are pinned. There is no automated guarantee a twelfth does not exist.",
      "recommendedFix": "A canonical taught `senseId` per entry that every language's representation derives from.",
      "effort": "MEDIUM (content) — a senseId per entry"
    },
    {
      "id": "I-11",
      "area": "Accessibility",
      "severity": "P2",
      "status": "RESOLVED",
      "summary": "Vocabulary listening questions relied on the hint ladder for a text alternative",
      "customerImpact": "Usable, but scored as a reveal rather than as an accommodation",
      "evidence": "There is no vocabulary listening question left to accommodate; §16.5. The letter exercises are I-21.",
      "recommendedFix": "done"
    },
    {
      "id": "I-21",
      "area": "Accessibility",
      "severity": "P2",
      "status": "OPEN",
      "summary": "`sound_recognition` and `distinguish` letter exercises are heard-only, and the toggle that skipped them is gone",
      "customerImpact": "A deaf learner arriving today meets letter questions they cannot answer. Anyone who had already turned the setting on keeps it — the stored `sound_free` flag is still honoured.",
      "evidence": "No sound-free control in My Learning; `candidates()` still reads `sound_free` and still filters the letter modes.",
      "recommendedFix": "A small per-question fallback on an audio-only letter question — \"Can't use audio?\" swapping in an equivalent visual recognition question, not revealing the answer and not penalised — rather than restoring a global setting.",
      "effort": "LOW–MEDIUM — one per-question fallback, in 32 languages"
    },
    {
      "id": "I-22",
      "area": "Vocabulary UX",
      "severity": "P3",
      "status": "OPEN",
      "summary": "A beginner's first sitting alternates two question layouts rather than four",
      "customerImpact": "Ten new words, two shapes. The variety returns within days as words reach `review` and `familiar`.",
      "evidence": "`stepsFor('new', i)` alternates `meaning` and `context`; two of the four checks were listening.",
      "recommendedFix": "A non-listening third check for new words, if one can be recognition-only. The accepted cost of §16.5.",
      "effort": "MEDIUM — a third non-listening check for new words"
    },
    {
      "id": "I-12",
      "area": "Persistence",
      "severity": "P2",
      "status": "OPEN",
      "summary": "No export: clearing site data destroys the history irrecoverably",
      "customerImpact": "A learner who clears browser data loses everything",
      "evidence": "A consequence of having no account and device-local persistence. §24.6.",
      "recommendedFix": "None that is customer-facing — a developer-style JSON export was tried and rejected. Keep IndexedDB robust, keep persistent storage requested, and do not warn normal users about it.",
      "effort": "NONE — closed by decision"
    },
    {
      "id": "I-13",
      "area": "Relations",
      "severity": "P2",
      "status": "OPEN",
      "summary": "243 of 2,581 words carry any verified lexical relation",
      "customerImpact": "Synonym and antonym sections rarely appear",
      "evidence": "`vocabulary:relations:qa`.",
      "recommendedFix": "Nothing, unless a conservative source can be found. Sparse trustworthy data is not a defect and inventing similar words would be.",
      "effort": "NONE unless a conservative source appears"
    },
    {
      "id": "I-15",
      "area": "Audio",
      "severity": "P3",
      "status": "RESOLVED",
      "summary": "마디 was mispronounced in one voice",
      "customerImpact": "One word sounded wrong",
      "evidence": "Regenerated, fixtured, checked on-device.",
      "recommendedFix": "done"
    },
    {
      "id": "I-16",
      "area": "Audio",
      "severity": "P3",
      "status": "RESOLVED",
      "summary": "The recogniser screen reported 낳다 as 낫다 in both voices",
      "customerImpact": "None — the recordings are correct. The open question was the defect.",
      "evidence": "The two readings differ measurably: 낳다 is [나타], an aspirated ㅌ with a short closure and a weak breathy release; 낫다 and 낮다 are both [낟따], a long closure and a sharp tense release. Measured off the shipped clips, both voices: 낫다 250/190 ms closure and −4.1/−2.9 dB release, 낮다 250/190 ms and −4.1/−2.8 dB, 낳다 170/170 ms and −6.9/−5.8 dB. The two [낟따] words are near-identical to each other and 낳다 is apart from both, in the direction aspiration predicts. `check_contrasts` in `qa_pronunciation.py` asserts this on every run, and fails if the pair is asserted the other way round.",
      "recommendedFix": "done — the recogniser is not a normative judge of a clip and no longer gates this word."
    },
    {
      "id": "I-17",
      "area": "i18n copy",
      "severity": "P2",
      "status": "OPEN",
      "summary": "No locale has been reviewed by a native speaker, across 32 interfaces",
      "customerImpact": "Unknown awkwardness in thirty-one languages, and in Korean",
      "evidence": "`docs/LOCALIZATION_NATIVE_REVIEW.md` states it. The severity was raised when the surface tripled.",
      "recommendedFix": "Native review. Nothing automated substitutes for it, and no document here may claim it has happened.",
      "effort": "HIGH (people, not engineering)"
    },
    {
      "id": "I-18",
      "area": "Content",
      "severity": "P3",
      "status": "OPEN",
      "summary": "103 glosses carry more than one sense in some language",
      "customerImpact": "차 is \"a car, or the tea you drink\" on a beginner's card",
      "evidence": "Reported by `vocabulary:sense:qa`; not gated.",
      "recommendedFix": "One card, one taught sense. Additional senses belong in the Word Detail block, not in the quiz answer.",
      "effort": "MEDIUM (content)"
    },
    {
      "id": "I-19",
      "area": "Vocabulary",
      "severity": "P1",
      "status": "OPEN",
      "summary": "Word meanings exist in ten of the thirty-two interface languages",
      "customerImpact": "Twenty-two languages read a fully translated app with English word cards",
      "evidence": "Stated on the row in the language picker before the learner chooses, which is what makes it a limitation rather than a misrepresentation. §23.3.",
      "recommendedFix": "Complete the shipping vocabulary content for the remaining twenty-two locales.",
      "effort": "HIGH (content) — 22 locales × 2,581 words"
    },
    {
      "id": "I-20",
      "area": "Vocabulary",
      "severity": "P3",
      "status": "OPEN",
      "summary": "The *More about it* block is written for 25 words",
      "customerImpact": "The other 2,556 word cards end at the example",
      "evidence": "The alternative was 784 dictionary scrapings, which is why they were deleted.",
      "recommendedFix": "Write it for the words that need it — polysemous, grammar-heavy, high-frequency functional, culturally specific, easily confused — starting with the top 500. Not a paragraph under every word.",
      "effort": "HIGH (content) — start at the top 500"
    },
    {
      "id": "I-26",
      "area": "Splash",
      "severity": "P3",
      "status": "OPEN",
      "summary": "The native launch screen shows the English wordmark in every locale",
      "customerImpact": "A Korean learner opening the Android app sees “Han gyul — Like a slice of tangerine, one letter a day” in English for the moment before the WebView paints, then the Korean artwork replaces it. Two wordmarks in two languages, one launch.",
      "evidence": "`SPLASH_SOURCE` in `scripts/content/build_app_icons.py` is `apps/common_assets/splash/splash_eng.png`, and every Android drawable and the iOS `Splash.imageset` are generated from it — confirmed by opening `drawable-port-xxxhdpi/splash.png`, which carries the English wordmark and tagline. The in-app splash is correct: `ui/LaunchSplash` reads `LocaleContext` and picks `splash-ko.png` for Korean, `splash-en.png` otherwise. The previous design handed over on a wordless frame of the splash animation; there is no animation any more and so no wordless frame to hand over from.",
      "recommendedFix": "Either generate a wordless variant of the still — the artwork's ground and radial wash with no type — and use that as the native bitmap on both platforms, or accept the flash and say so. Device locale is not the interface language and must not be used to guess.",
      "effort": "LOW — one wordless export, regenerated by `mobile:icons`"
    },
    {
      "id": "I-27",
      "area": "UI",
      "severity": "P3",
      "status": "OPEN",
      "summary": "Between 430 px and 560 px the bottom navigation floats clear of the screen edges",
      "customerImpact": "On a large phone in landscape, a small tablet or a split-screen window, the tab bar is 430 px wide on a wider page, so warm ground shows down both sides of it and it does not reach the bottom corners. It reads as a bar that has come loose from the app — the same symptom that was fixed above 560 px.",
      "evidence": "Measured at a 500 px viewport: `#root` is full-bleed at 500 px with no transform, radius or clipping, while the bottom navigation measures x=35, width=430. Above 560 px `#root` itself becomes the 430 px device and the nav fills it exactly (x=505, width=430 at 1440 px), which is correct. The band exists because `max-width: 430px` stays on `.shell` for 430–560 px while the desktop frame does not start until 560 px.",
      "recommendedFix": "Let the navigation span the viewport below 560 px while the content column stays capped at 430 px, or start the device frame at 430 px instead of 560 px. A CSS change in `AppShell.module.css` and `styles/global.css`, with a Playwright width case beside the existing safe-area ones.",
      "effort": "LOW — one media-query boundary"
    },
    {
      "id": "I-28",
      "area": "Build",
      "severity": "P3",
      "status": "OPEN",
      "summary": "`docs:consistency` cannot see four of the figures it tracks, and one of them had drifted",
      "customerImpact": "None to a learner. It matters because this report's credibility rests on its numbers, and a gate that ends with “No document states two different current values for the same metric” while a stale value sits in §2.3 reads as stronger than it is.",
      "evidence": "`npm run docs:consistency:check` passes and prints, for four of the thirteen metrics it tracks, `— not stated anywhere`: `unitTests 671`, `handwritingTests 95`, `apkMegabytes 63.5` and `aabMegabytes 62.2`. All four *are* stated — the APK and AAB sizes on the first table of §2, the test counts in §2.3 — so the checker is failing to match them, not tolerating an omission. One of the four had also gone stale: §2.3 said **664** web unit tests while the suite has been at **671**, and because the cell was invisible to the matcher nothing reported the drift. The previous report recorded this same checker as fixed for the APK/AAB rows (\"matches by extension\"); it does not match them now.",
      "recommendedFix": "Have the checker fail on a *missing* mention for any metric it is asked to track, rather than only on a contradictory one — a metric nothing states is a metric nothing is guarding. Then key the report's figure cells to metric names the way the issue tables are keyed to `issues.json`, so matching is exact instead of heuristic.",
      "effort": "LOW — one check, or one marker convention"
    },
    {
      "id": "I-29",
      "area": "Build",
      "severity": "P2",
      "status": "OPEN",
      "summary": "Two end-to-end tests fail, and no `verify` target runs the suite that would have said so",
      "customerImpact": "None directly — the failing assertion is about a mouse wheel on the Activity screen's range row, and the behaviour works a second after the screen opens. It matters because the previous report recorded `test:e2e` as PASS with both projects run in full, and this cycle it is 228 of 230.",
      "evidence": "`npx playwright test` over both projects: 228 passed, 2 failed in 10.8 minutes — `activity.spec.ts:144 a horizontal filter row can be reached with a mouse`, in `mobile` and in `desktop`. Reproducible, not flaky. Root cause found by instrumenting the page: `ui/LaunchSplash` renders above the router on every route, is `position: absolute; inset: 0; z-index: 100`, and only gets `pointer-events: none` once it is *leaving* — so for the 900 ms minimum hold it is what `document.elementFromPoint` returns over the whole app, and it swallows the wheel. Measured: at 0/200/400 ms the element at the row's centre is the splash `<img>`; from ~600 ms it is the chip. Wheeling after 1,000 ms moves the row 54 px as intended, so the feature is not broken. Playwright's `toBeVisible()` does not test occlusion, so the test proceeds while the splash is on top. The cause predates `e026697`, which shortened the hold from 1,800 ms to 900 ms — which makes the previous report's clean-sweep claim doubtful rather than merely stale. Separately: `verify:quick`, `verify:release` and `verify` contain no `test:e2e`, so 230 cases are outside the gate entirely.",
      "recommendedFix": "Two things, and the second is the one that matters. Give the splash `pointer-events: none` from the start — it has nothing to click and the app beneath it is already mounted — which fixes the test and the swallowed first tap together. Then put `test:e2e` on a gate, so a suite this size cannot drift out of green unnoticed.",
      "effort": "LOW — one CSS line and one gate entry"
    },
    {
      "id": "I-30",
      "area": "Docs",
      "severity": "P3",
      "status": "OPEN",
      "summary": "The report's screenshots have no working generator, and one had gone two cycles stale",
      "customerImpact": "None to a learner. It matters to anyone reading this report to decide what the product is: Figure 8 showed two vocabulary listening questions a few hundred lines below the prose explaining that none exists or can be generated.",
      "evidence": "The seven `audit-*.png` files the report embeds are produced by nothing in `scripts/` — `capture-report-shots.mjs` emits a different set and never mentions them, so they were captured by hand in some earlier cycle and never again. That script also no longer runs to completion: `node scripts/capture-report-shots.mjs` fails after 30 s on `getByRole('button', { name: /Animals & Nature/ })`, because the category tiles on `/words` became links. All seven were recaptured by hand in this audit against the build from `e026697`, which is a fix for one cycle and not a fix.",
      "recommendedFix": "Fold the `audit-*` set into `capture-report-shots.mjs`, repair its category selector, and have `docs:report` refuse to build when a referenced image is older than the newest commit touching `apps/web/src`. The build already hard-fails on a missing image; an out-of-date one should be no different.",
      "effort": "LOW–MEDIUM — one selector, seven capture steps, one staleness check"
    }
  ]
}
